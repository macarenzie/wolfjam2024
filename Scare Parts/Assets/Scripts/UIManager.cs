using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UIElements;

public class UIManager : MonoBehaviour
{
    // === Fields ===

    // Reference to the UIDocument
    private UIDocument UIDocument;

    // Reference to the health bar UI element
    private ProgressBar healthBar;

    // Reference to the fuel gauge stick
    private VisualElement fuelGaugeStick;

    // Reference to the fuel gauge background
    private VisualElement fuelGaugeBackground;


    // === Methods ===

    // Start is called before the first frame update
    void Start()
    {
        // Change color of the health bar to red


    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
